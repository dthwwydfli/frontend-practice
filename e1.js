//Declare a const called person as an object with a name and age. Log a sentence using both values.

const person = {
    name: "Istiaq",
    age: 20
};

console.log(person.name);
console.log(person.age);

